#include <stdio.h>
int fact(int);
int main()
{
    int n,ans;
    printf("enter a number \n");
    scanf("%d",&n);
    printf("factorial of %d= %d ",n,fact(n));
    return 0;
}
int fact(int n)
{
    if(n==1)
    return 1;
    else
    return(n*fact(n-1));
 }
